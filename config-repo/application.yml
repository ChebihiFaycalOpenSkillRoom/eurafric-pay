eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0  # en dev uniquement

spring:
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
